# このリポジトリで作業するときの取り決め

## 設計の経緯・不具合ログを読む

作業前に `docs/decisions.md`（設計判断ログ）、`docs/timeline.md`（開発タイムライン）、
`docs/bugs.md`（不具合・課題ログ）に目を通してください。特に以下のケースでは必須です。

- 認証・マスタ入力方式・画面構成（状態モード／入力モード／履歴）など、既に一度議論して
  決着した設計に触れる変更をするとき
- 「なぜ今の実装になっているか」が index.html や js/ のコードだけでは分からないとき

設計判断が新たに固まったとき、または不具合を直したときは、実装の変更と同じタイミングで
該当ファイルに追記してください（`docs/decisions.md` は判断と理由、`docs/bugs.md` は
症状と対応、`docs/timeline.md` は日付と出来事を1行）。会話の中で決まったことをその場で
書き残さないと、次の作業のときに経緯が失われるため。

## 検証スクリプトの実行範囲

`scripts/verify-*.mjs` は 90 本以上あり、全件実行には 5 分ほどかかります。
**全件実行は既定にしない**でください。

- 薬剤・検査マスタへの項目追加のような単純な作業では、関係するスクリプトを
  1〜2 本だけ個別に動かせば十分です。
  （例: マスタ追加なら `verify-med-oral-leaf-seed` と、対象カテゴリの UI 検証 1 本）
- 認証フロー・共通ヘルパー・広範な CSS の変更など、影響範囲が読めない変更で
  全件実行が必要だと判断した場合は、**勝手に走らせず先に確認を取る**こと。
  その際は「なぜ必要か」と「絞るならこの範囲」を添えて質問する。

## その他

- 応答は日本語で行う。
- 検証で再生成される `tools/` 配下のスクリーンショットは、原則コミットに含めない。
